import React from 'react';
import "./_LineGraph.scss";

export default function LineGraph(props) {
  const chartRef = React.createRef();

  
  return (
    <>
      <div className={"test"}>
        <div>test</div>
        {/* <canvas id="myChart" ref={props.chartRef} /> */}
      </div>
    </>
  );
}
